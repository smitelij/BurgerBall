<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eli.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".StartScreen"
            android:label="burgerball"
            android:theme="@style/AppTheme.NoActionBar">
            <!--android:theme="@style/AppTheme"> -->
            <!--android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen" > -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SetSelect"
            android:label="burgerball"
            android:theme="@style/AppTheme.NoActionBar"

            android:parentActivityName="com.example.eli.myapplication.StartScreen" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.eli.myapplication.StartScreen" />
            >



        </activity>

        <activity
            android:name=".MainActivity"
            android:label="burgerball"
            android:theme="@style/AppTheme.NoActionBar"

            android:parentActivityName="com.example.eli.myapplication.SetSelect" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.eli.myapplication.SetSelect" />>


        </activity>

        <activity
            android:name=".OpenGLES20Activity"
            android:label="burgerball"
            android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen"
            android:parentActivityName="com.example.eli.myapplication.MainActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.eli.myapplication.MainActivity" />

        </activity>


    </application>

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

</manifest>
